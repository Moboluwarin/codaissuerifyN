<tr class="song <%= song.title? ? "success" : "" %>" data-id="<%= song.id %>">
  <td>
    <input name="song[<%= song.id %>]" id="song-<%= song.id %>" type="checkbox" value="" >
  </td>
  <td>
    <label for="song-<%= song.id %>"><%= song.title %></label>
  </td>
</tr>
